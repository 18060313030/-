给父容器设置布局方式为flex之后，子容器会横向排列
  因为默认主轴在水平方向上，弹性盒子都是沿着主轴排列

align-items:stretch 要想生效，子级盒子上面不能定义高度
align-items:stretch是默认值，如果不设置高度，就会拉伸填满整个父容器的高度

可以在公共样式中给img标签选择器添加一个width:100% 这样子在写样式时，可以直接给img的父容器定义宽高，不需要再给img定义宽高，提升了灵活性

flex布局兼容性没有浮动布局来的好

IE9及以下，不支持flex